pipeline {
    agent any

    stages {
        stage('Clone') {
            steps {
                echo 'Cloning done automatically'
            }
        }

        stage('Build') {
            steps {
                echo 'Build stage running...'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing...'
            }
        }
    }
}
